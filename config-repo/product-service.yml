spring:
  datasource:
    url: jdbc:mysql://${PRODUCT_DB_HOST:127.0.0.1}:${PRODUCT_DB_PORT:3306}/product_service_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:${MYSQL_ROOT_PASSWORD:Akhil@1505}}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    url: jdbc:mysql://${PRODUCT_DB_HOST:127.0.0.1}:${PRODUCT_DB_PORT:3306}/product_service_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    user: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:${MYSQL_ROOT_PASSWORD:Akhil@1505}}
    enabled: true
    locations: classpath:db/migration